<% if (!user) { %>
  <link rel="stylesheet" href="/public/stylesheets/siwg.css">
  <% } %>
    </head>
    <!----------------------------------------------->

    <body>
      <nav class="nav">
        <% if (user) { %>
        <a href="/posts">
          <img src="/mainLogo.png" alt="image of a love letter" width="30" height="30">
        </a>
        <a class="nav-link" href="/posts">Feed</a>
        <a class="nav-link" href="/posts/new">Proclaim</a>
        <a class="nav-link" href="/auth/logout">Logout</a>
      </nav>
      <% } else { %>
        <a href="/auth/google" class="siwg-link">
          <button type="submit" class="siwg">
            <img id="siwg-image" alt="Sign in with Google" />
          </button>
        </a>
        <% } %>
          </nav>